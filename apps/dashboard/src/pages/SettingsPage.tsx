export default function SettingsPage() {
  return (
    <div className="page">
      <h2>Settings</h2>
      <p>
        Configuration for prompt schedules, symptom questions, event tags,
        medication lists, and feature flags.
      </p>
    </div>
  );
}
