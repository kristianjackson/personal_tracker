export default function OverviewPage() {
  return (
    <div className="page">
      <h2>Overview</h2>
      <p>
        Completion rate, check-in streak, note count, active flags, missing-data
        heatmap, and last check-in timestamp.
      </p>
    </div>
  );
}
