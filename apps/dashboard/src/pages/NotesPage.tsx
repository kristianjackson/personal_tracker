export default function NotesPage() {
  return (
    <div className="page">
      <h2>Notes</h2>
      <p>
        Note viewer with full-text search, tag filter chips, date range filter,
        and expandable note cards.
      </p>
    </div>
  );
}
